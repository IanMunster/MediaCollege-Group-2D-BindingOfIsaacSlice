using UnityEngine;
using System.Collections;
/// <summary>
/// Player aim animator.
/// </summary>
public class PlayerAimAnimator : MonoBehaviour {
	/*
	private Animator _animator;
	private PlayerStatus _state;

	// Use this for initialization
	void Start () {
		_animator = GetComponent<Animator> ();
		_state = GetComponentInParent<PlayerStatus> ();
	}
	
	// Update is called once per frame
	void Update () {
		Animate ();
	}

	void Animate(){
		_animator.SetInteger ("state", _state.getLookState);
		_animator.SetBool ("isShooting", _state.getShootState);
	}
	*/
}
