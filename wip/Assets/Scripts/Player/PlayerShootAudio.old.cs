using UnityEngine;
using System.Collections;
/// <summary>
/// Player shoot audio.
/// </summary>
public class PlayerShootAudio : MonoBehaviour {
	/*
	[SerializeField] private AudioClip[] _shootAudio;
	private AudioSource _shootSource;
	private PlayerStatus _status;

	private bool _isPlaying;
	private int _randomizer;

	// Use this for initialization
	void Start () {
		_status = GetComponentInParent<PlayerStatus> ();
		_shootSource = GetComponent<AudioSource> ();
	}
	
	// Update is called once per frame
	void Update () {
		if(_status.getShootState){
			print ("Play ShootSound");
			_shootSource.clip = _shootAudio[_randomizer];
			_shootSource.Play ();
		}
	}
	*/
}
